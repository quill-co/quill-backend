export const buildExtractionPrompt = () => {
  return `Extract up to 5 job listings from the page that are relevant to the profile of the job seeker. it's okay if they aren't an exact match, but they should be a good fit.`;
};
